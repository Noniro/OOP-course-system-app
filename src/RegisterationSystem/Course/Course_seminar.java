package RegisterationSystem.Course;

import RegisterationSystem.Teacher;

public class Course_seminar extends Course
{
     Course_seminar(String name, int number, int ID, Teacher teacher) {
        super(name, number, ID, teacher);
    }
}
